package pl.galajus.brokenpediabackend.admin.orb.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import pl.galajus.brokenpediabackend.admin.orb.model.AdminOrb;

public interface AdminOrbRepository extends JpaRepository<AdminOrb, Long> {
}
